// Shared configuration for external waitlist form
// This URL should be configured via environment variable in production
export const EARLY_ACCESS_WAITLIST_URL = 
  import.meta.env.VITE_WAITLIST_URL || 'https://forms.gle/GtpLSwaEUTKwq5Cs8';

